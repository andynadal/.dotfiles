- defaults:
     link:
       relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.config/bat:
      create: true
      path: config/bat

- create:
    - ~/dev

- shell:
  - 
    command: ./install-homebrew.zsh
    stdout: true
    stderr: true