- defaults:
     link:
       relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/dev

- shell:
  - 
    command: ./install-homebrew.zsh
    stdout: true
    stderr: true